import { Layout, Header, Hero, Display, Footer } from "../components";

export default function Index(){
    return (
        <Layout title={'Palettes - The super fast color palettes generator!'}>
            <Header/>
            <Hero/>
            <Display/>
            <Footer/>
        </Layout>
    )
}
